# Admin Dashboard

This directory contains the admin dashboard components for MantisDB.

## Structure

- `frontend/` - React-based web interface
- `api/` - REST API server for dashboard operations
- `assets/` - Static assets and embedded files
- `websocket/` - WebSocket handlers for real-time updates